using System;
using System.Linq;

namespace _Model
{
    public class Enums
    {
        public enum RegistroOrderBy
        {
            Sistema = 1,
            FechaAlta = 2
        }
        public enum Sistema
        {
            Sorteo = 1,
            AutoDeAlquilerConChofer = 2
        }
    }
}